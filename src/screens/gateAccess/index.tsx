import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GateAccess = () => {
  return (
    <View>
      <Text>GateAccess</Text>
    </View>
  );
};

export default GateAccess;

const styles = StyleSheet.create({});
