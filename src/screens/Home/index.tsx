import { SafeAreaView } from "../../components/layout/safeAreaView";
import QuickLinks from "./quickLinks";

const Home = () => {
  return (
    <SafeAreaView>
      <QuickLinks />
    </SafeAreaView>
  );
};

export default Home;
